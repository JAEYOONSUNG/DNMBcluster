% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pangenome_landscape.R
\name{plot_pangenome_landscape}
\alias{plot_pangenome_landscape}
\title{Pan-genome landscape composite (HOG occupancy + species-pair relationships)}
\usage{
plot_pangenome_landscape(
  out_dir,
  out_pdf = NULL,
  width = NULL,
  height = NULL,
  verbose = TRUE
)
}
\arguments{
\item{out_dir}{Directory produced by `run_orthofinder_like()`.}

\item{out_pdf}{Destination PDF.}

\item{width, height}{PDF dimensions (inches). Auto-scaled if NULL.}

\item{verbose}{Echo progress.}
}
\value{
Path to the written PDF (invisibly), or `NULL` on failure.
}
\description{
Stacks the two N-by-many heatmaps that describe the pan-genome from
complementary angles into one editorial figure:
}
\details{
\itemize{
  \item **Top** root-HOG copy-number heatmap with species tree.
        Reads how many copies of each HOG sit in each genome.
  \item **Bottom** species-pair triptych (orthologs / paralogs /
        xenolog candidates). Reads how genomes relate pairwise.
}

Both panels are produced via their respective standalone functions
with `out_pdf = FALSE` so this composite remains a thin orchestrator.
}
