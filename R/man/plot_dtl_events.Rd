% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dtl_reconcile.R
\name{plot_dtl_events}
\alias{plot_dtl_events}
\title{Aggregate DTL event counts onto the species tree and plot}
\usage{
plot_dtl_events(events_tbl, species_tree, losses_tbl = NULL, node_scale = 0.6)
}
\arguments{
\item{events_tbl}{Tibble with columns `cluster_id, g_node, sp_lca,
event, ...` as produced by `.run_dtl_batch()`. The `sp_lca`
column indexes nodes/tips of `species_tree`.}

\item{species_tree}{The rooted `ape::phylo` used for reconciliation.}

\item{losses_tbl}{Optional tibble of per-species-edge losses
(`sp_parent, sp_child, loss_count`). If supplied, edges are
drawn with widths proportional to cumulative loss count.}

\item{node_scale}{Scalar applied to node-pie radius (user units).
Default 0.6.}
}
\value{
Invisibly returns a tibble keyed by `sp_node` with columns
  `n_S, n_D, n_T, n_loss_in` (losses on the edge leading to the node).
}
\description{
Takes a per-OG events table produced by `.run_dtl_batch()` (or a
list of individual `reconcile_dtl()` results) and summarizes per
species-tree node: total speciations, duplications, transfer
candidates, and losses (via the paired loss tables). Draws a
species tree with node pies (S/D/T) and edge widths proportional
to loss counts.
}
