% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_orthofinder_like.R
\name{run_orthofinder_like}
\alias{run_orthofinder_like}
\title{Run the full OrthoFinder-parity workflow on a DNMB results directory}
\usage{
run_orthofinder_like(
  dnmb,
  out_dir,
  method = c("nj", "ml", "iqtree"),
  min_seqs = 3L,
  max_seqs = 500L,
  use_multi_copy = FALSE,
  outgroup = NULL,
  xeno_z = 3,
  threads = 2L,
  verbose = TRUE,
  refine_graph = FALSE,
  refine_method = c("louvain", "mcl", "walktrap"),
  bootstrap = 0L,
  force = FALSE,
  workers = 1L,
  species_tree_method = c("stag", "supermatrix", "auto"),
  trim_gaps = TRUE,
  max_gap_frac = 0.5,
  aa_model = "LG",
  gamma_k = 4L,
  og_bootstrap_reps = 0L,
  min_dup_support = 0,
  supermatrix_partitioned = FALSE,
  dtl = FALSE
)
}
\arguments{
\item{dnmb}{Result of `load_dnmb()`.}

\item{out_dir}{Destination directory.}

\item{method}{Tree method: `"nj"` (fast distance), `"ml"` (phangorn
NNI, accurate), or `"iqtree"` (IQ-TREE `--fast` + UFBoot, fastest
ML for large OGs when the `iqtree2`/`iqtree` binary is on PATH;
auto-falls back to phangorn ML otherwise).}

\item{min_seqs}{Skip OGs smaller than this.}

\item{max_seqs}{Subsample OGs larger than this.}

\item{use_multi_copy}{Include multi-copy OGs in STAG aggregation.}

\item{outgroup}{Force-rooting outgroup (overrides STRIDE).}

\item{xeno_z}{Xenolog-candidate Z-score cutoff.}

\item{threads}{Thread count for engines.}

\item{verbose}{Echo per-phase progress.}

\item{refine_graph}{If TRUE, pre-refine DNMB's identity-based OGs with
`refine_ogs_graph()` (length-normalized edges + community split)
before building gene trees. Closes OrthoFinder's RBH-reweighting gap.}

\item{refine_method}{Passed to `refine_ogs_graph()` when refine_graph=TRUE.}

\item{bootstrap}{Species-tree bootstrap replicates for STAG node support.}

\item{force}{If FALSE (default), reuse existing per-OG alignments and
trees on disk; if TRUE, rebuild everything. Passed to
`per_og_trees()` for resume-friendly reruns.}

\item{workers}{Parallel workers for per-OG tree building. Requires
the `future.apply` Suggests package when > 1.}

\item{species_tree_method}{Which species-tree builder to use:
`"stag"` (default — distance aggregation, handles multi-copy),
`"supermatrix"` (concatenated SC-OG alignment + ML/NJ), or
`"auto"` (supermatrix if >= 20 SC-OGs, else STAG).}

\item{trim_gaps}{Drop high-gap alignment columns before tree building
(passed to `per_og_trees`). Default TRUE.}

\item{max_gap_frac}{Column-gap threshold for `trim_gaps`. Default 0.5.}

\item{aa_model}{Amino-acid model for ML branch. Default `"LG"`.}

\item{gamma_k}{Γ rate categories for ML. Default 4 (0 disables).}

\item{og_bootstrap_reps}{Per-OG bootstrap replicates (0 disables).
Support percentages land on `tree$node.label` in each OG tree.}

\item{min_dup_support}{When `og_bootstrap_reps > 0`, gene-tree
duplication nodes with bootstrap support below this percentage are
ignored by STRIDE rooting and HOG cutting. Default 0.}

\item{supermatrix_partitioned}{If TRUE and ML supermatrix is used,
fit each SC-OG as its own partition via `phangorn::pmlPart`. Falls
back to concatenated ML on error. Default FALSE.}

\item{dtl}{If TRUE, run per-OG DTL reconciliation via `reconcile_dtl()`
and write `dtl_per_og.tsv` + `dtl_events.tsv` to `out_dir`. Default
FALSE — the reconciliation is a heuristic LCA pass, not a cost-space
search; for rigorous DTL use `export_og_bundles()` + GeneRax/ALE.}
}
\value{
Named list with keys `og_result`, `species_tree`, `hogs`,
  `relationships`, `dtl` (if `dtl = TRUE`), and `refined` (if `refine_graph`).
}
\description{
One-shot wrapper that chains the four phase functions:
\enumerate{
  \item `per_og_trees()`             — per-OG alignment + gene trees
  \item `stag_species_tree()`        — species tree from gene trees
  \item `stride_root()`              — duplication-consistent rooting
  \item `cut_hogs()`                 — HOG tables per internal node
  \item `reconcile_relationships()`  — ortholog / paralog / xenolog labels
}
}
\details{
Phase E-a (tool exports) is a separate call because not every user
wants GeneRax/PAML/MCScanX bundles on every run.

All output lands under `out_dir`:
\preformatted{
  orthogroups/OG_<id>/{aln.fasta,tree.nwk}
  orthogroup_trees.tsv
  single_copy_OGs.tsv
  species_tree_rooted.nwk
  HOGs/N<node>_<clade>.tsv
  relationships.parquet
}
}
