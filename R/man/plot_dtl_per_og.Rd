% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dtl_per_og.R
\name{plot_dtl_per_og}
\alias{plot_dtl_per_og}
\alias{plot_dtl_event_histogram}
\alias{plot_dtl_dt_coupling}
\alias{plot_dtl_concentration}
\alias{plot_dtl_top_ogs}
\alias{plot_dtl_per_og_summary}
\title{Per-OG DTL figures — one PDF per question}
\usage{
plot_dtl_event_histogram(out_dir, out_pdf = NULL, verbose = TRUE)

plot_dtl_dt_coupling(out_dir, out_pdf = NULL, verbose = TRUE)

plot_dtl_concentration(out_dir, out_pdf = NULL, verbose = TRUE)

plot_dtl_top_ogs(out_dir, out_pdf = NULL, top_n = 15L, verbose = TRUE)

plot_dtl_per_og_summary(out_dir, top_n = 15L, verbose = TRUE)
}
\arguments{
\item{out_dir}{`run_orthofinder_like()` directory containing
`dtl_per_og.tsv` (produced when `dtl = TRUE`).}

\item{out_pdf}{Destination PDF. If `NULL`, a sensible default is
written next to the TSV.}

\item{verbose}{Echo progress.}

\item{top_n}{Number of OGs to show on the bar panels (default 15).}
}
\value{
Path to the written PDF (invisibly), or `NULL` on failure.
}
\description{
`plot_duplication_burden()` focuses on duplications (D). This family
of plots widens the lens to cover the four reconciliation event
classes that `reconcile_dtl()` writes to `dtl_per_og.tsv`:
}
\details{

}
