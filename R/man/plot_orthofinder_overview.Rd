% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_orthofinder_overview.R
\name{plot_orthofinder_overview}
\alias{plot_orthofinder_overview}
\title{Multi-panel overview of a `run_orthofinder_like()` directory}
\usage{
plot_orthofinder_overview(out_dir, out_pdf = NULL, verbose = TRUE)
}
\arguments{
\item{out_dir}{Directory produced by `run_orthofinder_like()`.}

\item{out_pdf}{Destination PDF.}

\item{verbose}{Echo progress.}
}
\value{
Path to the written PDF (invisibly) or `NULL` on failure.
}
\description{
One PDF that tells a first-time reader what came out of the run:
}
\details{
\itemize{
  \item **A** rooted species tree (branch lengths + internal node IDs).
  \item **B** OG size distribution (log-x histogram).
  \item **C** HOG counts per internal species-tree node.
  \item **D** Per-branch loss totals from `dtl_losses.tsv` when
        `dtl = TRUE` was supplied; placeholder otherwise.
}

Anvi'o palette / theme via `.dnmb_anvio_pal` so the panel composes
cleanly with the rest of the plotting family.
}
