% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hog_occupancy.R
\name{plot_hog_occupancy_heatmap}
\alias{plot_hog_occupancy_heatmap}
\title{Root-HOG occupancy / copy-number heatmap}
\usage{
plot_hog_occupancy_heatmap(
  out_dir,
  out_pdf = NULL,
  min_prevalence = 2L,
  sort_by = c("prevalence", "cluster_id"),
  width = NULL,
  height = NULL,
  verbose = TRUE
)
}
\arguments{
\item{out_dir}{`run_orthofinder_like()` output directory.}

\item{out_pdf}{Destination PDF.}

\item{min_prevalence}{Minimum leaf count for a HOG to be plotted.}

\item{sort_by}{`"prevalence"` (core-like first) or `"cluster_id"`.}

\item{width, height}{Override auto-scaled PDF dimensions.}

\item{verbose}{Echo progress.}
}
\value{
Path to the written PDF (invisibly), or `NULL` on failure.
}
\description{
Side-by-side panel: rooted species tree (left) + HOG copy-number
heatmap (right). Core HOGs render as solid horizontal bands across
all tips; accessory HOGs as sparse columns. HOGs with prevalence
below `min_prevalence` are dropped to keep the long singleton tail
from washing out the figure.
}
\details{
Anvi'o palette / theme via `.dnmb_anvio_pal` for visual consistency
with the rest of the plotting family.
}
