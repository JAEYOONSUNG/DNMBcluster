% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/per_og_tree_grid.R
\name{per_og_tree_grid}
\alias{per_og_tree_grid}
\title{Faceted grid of per-orthogroup gene trees}
\usage{
per_og_tree_grid(
  og_result,
  top_n = 24L,
  ncol = 4L,
  prefer = c("largest", "single_copy", "random"),
  max_tip_label = 30L
)
}
\arguments{
\item{og_result}{Tibble returned by `per_og_trees()`.}

\item{top_n}{How many orthogroups to show (default 24, a 6x4 grid).}

\item{ncol}{Grid column count.}

\item{prefer}{Selection order: `"largest"` (most members), `"single_copy"`,
or `"random"`.}

\item{max_tip_label}{Max tips below which tip labels are drawn.
Larger trees only get tip points. Default 30.}
}
\value{
A ggplot / patchwork object. Caller saves via `ggsave()`.
}
\description{
Plots the first `top_n` trees from `per_og_trees()` output as a small-
multiple ggtree grid. Useful as a QC thumbnail and as a figure in
comparative-genomics write-ups.
}
\details{
Each panel shows the gene tree with genome-label tips coloured by
genome, a scale bar, and a header strip carrying the OG id, member
count and a single-copy flag when applicable.
}
