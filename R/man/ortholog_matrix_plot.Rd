% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ortholog_matrix_plot.R
\name{ortholog_matrix_plot}
\alias{ortholog_matrix_plot}
\title{Heatmap of per-species-pair ortholog count}
\usage{
ortholog_matrix_plot(
  rel,
  event = c("ortholog", "paralog", "xenolog"),
  log1p_fill = TRUE
)
}
\arguments{
\item{rel}{Tibble from `reconcile_relationships()`.}

\item{event}{One of `"ortholog"`, `"paralog"`, or `"xenolog"`. Selects
the event type to tally. `"xenolog"` uses `is_xenolog_candidate`.}

\item{log1p_fill}{Logical; if TRUE, map `log1p(n)` to fill for wide dynamic range.}
}
\value{
ggplot object.
}
\description{
Collapses the `reconcile_relationships()` output to a species x species
matrix of ortholog pair counts, and plots it as a ggplot tile map. Rows
and columns are ordered by `hclust` on the log counts for readability.
}
