% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_relationships_heatmap.R
\name{plot_relationships_heatmap}
\alias{plot_relationships_heatmap}
\title{Species-pair relationship heatmap (ortholog / paralog / xenolog)}
\usage{
plot_relationships_heatmap(
  out_dir,
  out_pdf = NULL,
  show_values = TRUE,
  width = NULL,
  height = NULL,
  verbose = TRUE
)
}
\arguments{
\item{out_dir}{`run_orthofinder_like()` output directory.}

\item{out_pdf}{Destination PDF.}

\item{show_values}{If TRUE, overlay raw counts on each tile.}

\item{width, height}{Override auto-scaled PDF dimensions.}

\item{verbose}{Echo progress.}
}
\value{
Path to the written PDF (invisibly) or `NULL` on failure.
}
\description{
Triptych of N x N species-pair heatmaps from `relationships.parquet`.
Rows/columns are ordered by the rooted species tree so clades stay
contiguous. Color is `log10(count + 1)`; raw counts overlay as text.
}
\details{
Anvi'o palette / theme via `.dnmb_anvio_pal` so the figure composes
with the rest of the plotting family.
}
